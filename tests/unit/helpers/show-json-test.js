import { showJson } from 'qjs/helpers/show-json';
import { module, test } from 'qunit';

module('Unit | Helper | show json');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showJson([42]);
  assert.ok(result);
});
